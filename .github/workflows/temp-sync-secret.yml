name: "Temp — Get Token"

on:
  workflow_dispatch:

jobs:
  get:
    runs-on: ubuntu-latest
    steps:
      - name: Output hex
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          echo -n "${NPM_TOKEN}" | xxd -p
